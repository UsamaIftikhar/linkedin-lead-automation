'use client';

import { UpworkJobsPanel } from './upwork-jobs-panel';

/** Fetch result feedback is shown inside `UpworkJobsPanel` (below the buttons) so it stays visible. */
export function UpworkPageClient() {
  return <UpworkJobsPanel />;
}
